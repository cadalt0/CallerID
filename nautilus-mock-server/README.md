# Nautilus Mock Server

A mock server for Nautilus TEE processing.

## Quick Start

```bash
cd nautilus-mock-server
npm i --force
npm run dev
```

The mock server will start on `http://localhost:3000`

